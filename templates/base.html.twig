<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Application{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
<div class="slogan bg-dark text-white py-3 text-center">
    <h2>Location de Véhicules</h2>
</div>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <!-- Bouton Accueil -->
        <a class="navbar-brand" href="{{ path('home') }}">Accueil</a>

        <!-- Bouton Véhicules -->
        <a class="navbar-brand" href="{{ path('vehicle_list') }}">Véhicules</a>

        <!-- Toggle button pour petits écrans -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <!-- Ajouter un bouton Login -->
                <li class="nav-item">
                    <a class="navbar-brand" href="{{ path('app_login') }}">Connexion</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

{% block body %}{% endblock %}

<footer class="text-center bg-dark text-white py-3">
    © 2023 Location de Véhicules
</footer>
</body>
</html>
